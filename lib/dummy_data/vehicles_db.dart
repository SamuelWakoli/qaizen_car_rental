//this is the model
class Vehicle {
  String numPlate, isAvailable, image, name, price, description, color, seats;

  Vehicle({
    required this.numPlate,
    required this.isAvailable,
    required this.image,
    required this.name,
    required this.price,
    required this.description,
    required this.color,
    required this.seats,
  });
}
